{
  "name": "dota2-rune-timer",
  "dependencies": {
    "fastify": "^2.7.1",
    "tcp-ping": "^0.1.1"
  },
  "bin": "index.js",
  "scripts": {
    "build-win": "pkg --out-path build --targets node10-win-x64 ."
  }
}
